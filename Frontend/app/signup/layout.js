export const metadata = {
  title: "SignUp Page",
  description: "Sign Up page allows the user to get Registered on the website",
};

export default function SignupLayout({ children }) {
  return (
    <>
        {children}
    </>
  );
}